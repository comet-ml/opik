"""Prompt templates for Parameter Optimizer.

Currently empty - Parameter Optimizer does not use LLM prompts.
It optimizes model parameters (temperature, top_p, etc.) rather than prompt text.
"""

DEFAULT_PROMPTS: dict[str, str] = {}
