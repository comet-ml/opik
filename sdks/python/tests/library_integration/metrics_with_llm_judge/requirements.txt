openai
openai-agents
langchain
langchain_openai
ragas<0.3.2; python_version<"3.10"
# ragas 0.4.x imports from langchain_community.chat_models.vertexai,
# which was removed in langchain-community 0.3.30+ (migrated to the
# standalone langchain-google-vertexai package). Pin to the 0.3.x line
# until ragas catches up or we rework the test to skip the vertexai
# code path entirely.
ragas<0.4; python_version>="3.10"
pillow # for ragas 0.3.0 to work
litellm[google] # for vertexai models
