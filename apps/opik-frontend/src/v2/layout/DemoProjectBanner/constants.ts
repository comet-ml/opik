// Matches the h-8 in the markup; the layout is told this before the bar is
// measured, because visibility resolves from a query and the bar appears after
// mount.
export const DEMO_BANNER_HEIGHT = 32;
