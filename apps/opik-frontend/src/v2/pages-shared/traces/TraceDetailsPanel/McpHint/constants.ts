// The control is not offered because an error exists — it is offered because the
// user chose to open it. The fade is what makes it read as a response rather
// than as furniture that was there all along.
// The grace for crossing the gap between the pill and the card. HoverCard keeps
// the card open for this long after the pointer leaves either of them, so a
// cursor in transit — or one clipping a corner — never dismisses it.
export const MCP_HINT_CLOSE_DELAY_MS = 260;

export const MCP_HINT_LABEL = "Fix via MCP";

export const MCP_HINT_DOCS_PATH = "/mcp-server";

export const MCP_HINT_TITLE = "Opik MCP";

export const MCP_HINT_DESCRIPTION =
  "Instead of writing a script for each question, your agent reads Opik directly — and can fix the code in the same step.";
