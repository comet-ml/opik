---
title: LLM Judge Presets
---

# LLM Judge Presets


> Current module: `opik.evaluation.metrics`


These presets wrap common GEval prompt templates so you can instantiate judges with
one line of code. Provide an appropriate `model` (for example, `"gpt-4o"`) and
ensure the backing provider is configured via `opik.configure`.

## Agent & Task Presets


> API details for `AgentTaskCompletionJudge` are available in the generated Core API pages.

    :special-members: __init__
    :members: score

> API details for `AgentToolCorrectnessJudge` are available in the generated Core API pages.

    :special-members: __init__
    :members: score

## Conversation Quality Presets


> API details for `DialogueHelpfulnessJudge` are available in the generated Core API pages.

    :special-members: __init__
    :members: score

> API details for `QARelevanceJudge` are available in the generated Core API pages.

    :special-members: __init__
    :members: score

> API details for `SummarizationCoherenceJudge` are available in the generated Core API pages.

    :special-members: __init__
    :members: score

> API details for `SummarizationConsistencyJudge` are available in the generated Core API pages.

    :special-members: __init__
    :members: score

> API details for `PromptUncertaintyJudge` are available in the generated Core API pages.

    :special-members: __init__
    :members: score

## Risk & Bias Presets


> API details for `ComplianceRiskJudge` are available in the generated Core API pages.

    :special-members: __init__
    :members: score

> API details for `DemographicBiasJudge` are available in the generated Core API pages.

    :special-members: __init__
    :members: score

> API details for `GenderBiasJudge` are available in the generated Core API pages.

    :special-members: __init__
    :members: score

> API details for `PoliticalBiasJudge` are available in the generated Core API pages.

    :special-members: __init__
    :members: score

> API details for `RegionalBiasJudge` are available in the generated Core API pages.

    :special-members: __init__
    :members: score

> API details for `ReligiousBiasJudge` are available in the generated Core API pages.

    :special-members: __init__
    :members: score
